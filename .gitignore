# Terraform state files (contain sensitive data)
*.tfstate
*.tfstate.backup
.terraform/
.terraform.lock.hcl

# AWS credentials and keys
*.pem
*.key
*.aws/
.aws/

# Terraform variable overrides (if any secrets are stored here)
terraform.tfvars

# Bash history and logs
*.log
*.bak
*.swp

# Python virtual environments (if using Checkov or other Python tools)
__pycache__/
*.py[cod]
*.egg-info/
venv/
.env

# OS-specific files
.DS_Store
Thumbs.db

# Checkov reports (if saving scan outputs)
checkov_report.json
checkov_report.txt
